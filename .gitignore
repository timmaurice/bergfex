# Local AI context files, personal to whoever is working here
CLAUDE.md

# Docker test environment
docker-compose.yml
config/
*.pem

# Python
*__pycache__*
.pytest_cache/

# Frontend build tooling
node_modules/
bun.lock

# Release artifact, rebuilt by the release workflow
bergfex.zip

# Playwright output
playwright-report/
test-results/

# Playwright session state. These hold live Home Assistant access and refresh
# tokens, so they are matched by name at any depth rather than by path.
.auth.json
.storage-state.json
